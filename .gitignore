# === Dependencies ===
node_modules/
.pnpm-store/

# === Build output ===
dist/
dist-ssr/
*.local
*.tsbuildinfo

# === Environment / secrets ===
.env
.env.*
!.env.example

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Tauri / Rust ===
src-tauri/target/
src-tauri/gen/
*.pdb
*.d
src-tauri/binaries/aether
src-tauri/binaries/aether.exe
src-tauri/binaries/aether-*
src-tauri/binaries/*.tar.gz
src-tauri/binaries/*.zip
src-tauri/binaries/SHA256SUMS.txt
# Aether writes its provisioned device identity (private keys, access
# tokens) here when run with binaries/ as its cwd (e.g. manual testing) —
# must never be committed.
src-tauri/binaries/*.toml
src-tauri/binaries/android/
# Complete fetched engine, PT companions and transactional staging/backups.
src-tauri/binaries/engine/
src-tauri/binaries/pt/
src-tauri/binaries/.engine-*/
src-tauri/binaries/__pycache__/

# === IDE / Editor ===
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# === OS ===
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# === Testing / Coverage ===
coverage/
.nyc_output/

# === Misc ===
.mimocode/
/ci-artifacts/
.claude/worktrees/
