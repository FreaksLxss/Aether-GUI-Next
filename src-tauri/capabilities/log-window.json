{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "log-window",
  "description": "Capability for the log viewer window",
  "windows": ["log-window"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close"
  ]
}
